# Flags: kyo-kernel main sources, cdefdc9e60..tip

Generated by `flags.sh cdefdc9e60..HEAD -- kyo-kernel/{shared,jvm-native,js-wasm}/src/main/scala`, then
every row adjudicated. A verdict is a category from the cast ladder, a measurement, a `moved`
provenance, or `REMOVE`; nothing else counts. Rows F6 and F7 are the classes the script cannot emit
(a claim with no number, a cost on a hot path) and are added by hand.

| id | site | added line | class | verdict |
|----|------|------------|-------|---------|
| F1 | ArrowEffect.scala:213 | `new Handler.ContHandler[I, O, E, A, A, S & S2]:` | allocation | justified: the `resumed` twin of `handleContRepeated`'s handler, a `val` built once with the handler at region entry, never on an answer or a resumption; a repeated region already allocates its handler and its `HandleArrow`, this is one object beside them |
| F2 | ArrowEffect.scala:274 | `new Handler.ContHandler[I, O, E, A, A, S & S2]:` | allocation | justified: as F1, for the recovering overload |
| F3 | ArrowEffect.scala:990 | `new Handler.ContHandler[I, O, E, A \| First, A \| First, S & S2]:` | allocation | justified: as F1, for `handleFirstRepeated`; `escaping` and `repeated` carried over so the twin's region owes and holds exactly as the original does |
| F4 | Handler.scala:105 | `new Arrow.Step[O[V], A, E & S]:` | allocation | justified: one arrow per operation answered by a repeated handler, built in `Eval`'s cont arm only when `handler.repeated`; the single-shot `handleCont` path allocates nothing new. Number: the benchmark section of the package, every row, base against tip |
| F5 | Handler.scala:109 | `case p: Pending[O[V], S3] @unchecked => Effect.defer(p, this, cont2)` | cast | erasure-forced: a typed pattern binding at the arm's type, the runtime test being `Pending` alone; the same shape and the same arm as `Arrow.apply`'s and `Suspend.crossing`'s pending case |
| F6 | Eval.scala, cont arm | `val continuation = if handler.repeated then handler.reentering(raw) else raw` | hot-path cost | measurement pending: `repeated` is a virtual `Boolean` on the cont arm, which every `handleCont` answer takes; overridden in three anonymous classes, so not devirtualised by class hierarchy analysis. The whole `KernelBench` class runs base against tip; the rows that answer through `handleCont` are the ones that would move. If they move outside drift, the recorded alternative is a marker trait tested by `isInstanceOf`, which is a new type and needs that number to justify it |
| F7 | Handler.scala:93 | `def resumed: ContHandler[I, O, E, A, A, S] = bug(...)` | claim | justified by construction, with the reach stated: only `reentering` calls `resumed`, and `Eval` calls `reentering` only under `handler.repeated`; the three handlers that override `repeated` all override `resumed`. A single-shot handler reaching it is a kernel invariant broken, which is what `bug` is for |

Not flagged and worth a line: the `outer =>` self alias in the three expansions is what lets the
twin's `run` delegate to the handler's own rather than duplicate the clause body, which is the
"overloads delegate to canonical" rule applied to an anonymous class.
