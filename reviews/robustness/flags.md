# Flags: kyo-kernel main sources, cdefdc9e60..tip

Generated by `flags.sh cdefdc9e60..HEAD -- kyo-kernel/{shared,jvm-native,js-wasm}/src/main/scala`, then
every row adjudicated. A verdict is a category from the cast ladder, a measurement, a `moved`
provenance, or `REMOVE`; nothing else counts. The script's classes cover every added line of concern
in this range; no hand-added row is needed, because `Eval` is unchanged and no member with a partial
default exists.

| id | site | added line | class | verdict |
|----|------|------------|-------|---------|
| F1 | Handler.scala:333 | `new ContHandler[I, O, E, A, A, S]:` | allocation | measured on this shape: the re-entered handler, built once per region entry by `Handler.reentered(outer)` and captured by the handler's `val`. `repeatedRegionsPayEntry` and `repeatedRegionsPayEntryRecovering` (a `handleContRepeated` region per operation, 1000 per invocation, through each overload), base against tip, `-f 3 -prof gc`, `bench/compare-rows-base-vs-tip.md`: 88,104 B/op against 104,120 B/op on both rows, 16 bytes per region, the one object; time 56.2 us against 59.9 us (+6.6%) and 59.0 us against 60.5 us (+2.6%), 3.7 and 1.5 ns per region. The session's base leg ran beside a compile, so the clean rerun of the three rows, `bench/compare-rows-base-vs-tip-clean.md`, replaces the time cells |
| F2 | Handler.scala:352 | `new Arrow.Step[O[X0], A, E & S]:` | allocation | measured on this shape, and the number is a regression on a row no consumer has: one arrow per suspension a repeated handler answers, built in the handler's `run`, and one region entered per application. `repeatedClausesPayReentry` (`suspensionBaseline` under `handleContRepeated`, 10,000 operations, each resumed once), base against tip, `-f 3 -prof gc`, `bench/compare-rows-base-vs-tip.md`: 480,121 B/op against 720,141 B/op, 24 bytes per operation (64 on the earlier shape, whose wrap lived in `Eval`); time 100.8 us (the base at `-f 1`, `bench/compare-base-vs-tip.md`; the session's `-f 3` base leg carries a 39% error from a compile run beside it) against 657.1 us, 56 ns per operation, 6.5 times. The clean rerun, `bench/compare-rows-base-vs-tip-clean.md`, replaces the time cells. The mechanism, the consumer's numbers and the decision are in the package's benchmark section and its open ruling 5; this row is not closed by this table |
| F3 | Handler.scala:356 | `case p: Pending[O[X0], S3] @unchecked => Effect.defer(p, this, cont2)` | cast | erasure-forced: a typed pattern binding at the arm's type, the runtime test being `Pending` alone; the same shape and the same arm as `Arrow.apply`'s and `Suspend.crossing`'s pending case |
| F4 | Handler.scala:399 | `else outcome.asInstanceOf[Outcome[A < (E & S), B < S] < S]` | cast | moved: the pass-through cast `LoopHandler.answers` and `answersLoop` each carried at their tail (`o.asInstanceOf[Outcome[A < (E & S), B < S] < S]` at the base), written once in `attachReentryToPending`. Category: representation assertion. The two outcome types differ only in the `Continue` payload, `O[X0] < (E & S)` against `A < (E & S)`; a settled outcome reaching the tail is not a `Continue`, the caller having answered that arm in place, so the value inhabits both and the cast records that rather than converts |
| F5 | Handler.scala:426 | `else outcome.asInstanceOf[Outcome2[State, A < (E & S), B < S] < S]` | cast | moved: as F4, from the tails of `LoopStateHandler.answers` and `answersLoopState`, for the state-carrying outcome |
| F6 | Handler.scala:491 | `result = attachReentryToPending[I, O, E, A, B, S, C](k.asInstanceOf[Arrow[O[C], A, E & S]], o)` | cast | moved: the same `k.asInstanceOf[Arrow[O[C], A, E & S]]` on the same site at the base (`answersLoop`'s tail, where it was the argument to `attachReentry`); only the call around it changed. Category: erasure-forced, the fused walk rebinding `k` per operation at the erased element type |
| F7 | Handler.scala:569 | `result = attachReentryToPending2[State, I, O, E, A, B, S, C](k.asInstanceOf[Arrow[O[C], A, E & S]], o2)` | cast | moved: as F6, for `answersLoopState`'s tail |

Not flagged and worth a line: `reentered`'s `override def repeated = true`. It is what holds a bracket
captured inside a re-entered region across that clause's resumptions, released where the re-entered
region ends, once the last of them has run; the scaladoc on `reentered` says so and a `BracketTest`
case pins it.

Also not flagged: the `inline` on `attachReentryToPending` and its state-carrying form. The two fused walks are
themselves `inline` templates expanded at every `handleLoop` and `handleLoopState` site, and their
size is a design property; a non-inline helper on the tail would add a call inside every expansion.
`inline` keeps the expansion the branch the walks carried before. Number: the fix leg against the
tip with C, `KernelBench` in full, same session, `-f 1`, 49 rows, `bench/compare-A-vs-AC.md`: zero
suspects; the rows the tails sit in move `handleLoopAnswersInPlace` +0.4%,
`handleLoopFusesContinuation` +0.0%, `statefulAnswersPaySuccessor` -0.9%, and every fusion row
within 2%. The two rows outside the band, `pureIterationViaArrow` -6.3% and
`continuationBodiesFuse` -5.9%, are inside their combined errors, and are confirmed at `-f 3` in
`bench/compare-A-vs-AC-f3.md`, zero suspects. The tails are the same on this tip as in that session.
