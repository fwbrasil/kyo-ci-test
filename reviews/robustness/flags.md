# Flags: kyo-kernel main sources, cdefdc9e60..tip

Generated by `flags.sh cdefdc9e60..HEAD -- kyo-kernel/{shared,jvm-native,js-wasm}/src/main/scala`, then
every row adjudicated. A verdict is a category from the cast ladder, a measurement, a `moved`
provenance, or `REMOVE`; nothing else counts. Rows H1 and H2 are the classes the script cannot emit
(a cost on a hot path, a claim with no number) and are added by hand, their ids outside the script's
`F` series so `package-check.sh` compares the script's count with the table's like with like.

| id | site | added line | class | verdict |
|----|------|------------|-------|---------|
| F1 | ArrowEffect.scala:213 | `new Handler.ContHandler[I, O, E, A, A, S & S2]:` | allocation | measured: the `resumed` twin, a `val` built once with the handler at region entry. `repeatedRegionsPayEntry` (a `handleContRepeated` region per operation, 1000 per invocation), base against tip, `-f 3 -prof gc`, `bench/compare-rows-base-vs-AC.md`: `gc.alloc.rate.norm` 88,104 B/op against 104,120 B/op, 16 bytes per region, the twin; time 53.7 us against 58.4 us, +8.8%, 4.7 ns per region, outside the 5% band and outside error |
| F2 | ArrowEffect.scala:274 | `new Handler.ContHandler[I, O, E, A, A, S & S2]:` | allocation | measured: as F1, the same object for the recovering overload; the row enters the plain overload, and the twin is the same anonymous shape with the same fields |
| F3 | ArrowEffect.scala:990 | `new Handler.ContHandler[I, O, E, A \| First, A \| First, S & S2]:` | allocation | measured: as F1, the same object for `handleFirstRepeated`, plus `escaping` and `repeated` carried over so the twin's region owes and holds exactly as the original does |
| F4 | Handler.scala:105 | `new Arrow.Step[O[V], A, E & S]:` | allocation | measured, and the number is a regression: one arrow per operation answered by a repeated handler, and one region node per application. `repeatedClausesPayReentry` (`suspensionBaseline` under `handleContRepeated`, 10,000 operations, each resumed once), base against tip, `-f 3 -prof gc`, `bench/compare-rows-base-vs-AC.md`: 480,121 B/op against 1,120,181 B/op, 64 bytes per operation; time 98.7 us against 706.4 us, +615%, 61 ns per operation. The single-shot `handleCont` path allocates nothing new (every other row in `bench/compare-base-vs-AC.md`). The mechanism and what is done about it are in the package's benchmark section; this row is not closed by this table |
| F5 | Handler.scala:109 | `case p: Pending[O[V], S3] @unchecked => Effect.defer(p, this, cont2)` | cast | erasure-forced: a typed pattern binding at the arm's type, the runtime test being `Pending` alone; the same shape and the same arm as `Arrow.apply`'s and `Suspend.crossing`'s pending case |
| F6 | Handler.scala:386 | `else outcome.asInstanceOf[Outcome[A < (E & S), B < S] < S]` | cast | moved: the pass-through cast `LoopHandler.answers` and `answersLoop` each carried at their tail (`o.asInstanceOf[Outcome[A < (E & S), B < S] < S]` at the base), written once in `attachReentryUnlessSettled`. Category: representation assertion. The two outcome types differ only in the `Continue` payload, `O[X0] < (E & S)` against `A < (E & S)`; a settled outcome reaching the tail is not a `Continue`, the caller having answered that arm in place, so the value inhabits both and the cast records that rather than converts |
| F7 | Handler.scala:413 | `else outcome.asInstanceOf[Outcome2[State, A < (E & S), B < S] < S]` | cast | moved: as F6, from the tails of `LoopStateHandler.answers` and `answersLoopState`, for the state-carrying outcome |
| F8 | Handler.scala:478 | `result = attachReentryUnlessSettled[I, O, E, A, B, S, C](k.asInstanceOf[Arrow[O[C], A, E & S]], o)` | cast | moved: the same `k.asInstanceOf[Arrow[O[C], A, E & S]]` on the same site at the base (`answersLoop`'s tail, where it was the argument to `attachReentry`); only the call around it changed. Category: erasure-forced, the fused walk rebinding `k` per operation at the erased element type |
| F9 | Handler.scala:556 | `result = attachReentryUnlessSettled2[State, I, O, E, A, B, S, C](k.asInstanceOf[Arrow[O[C], A, E & S]], o2)` | cast | moved: as F8, for `answersLoopState`'s tail |
| H1 | Eval.scala, cont arm | `val continuation = if handler.repeated then handler.reentering(raw) else raw` | hot-path cost | measured, inside drift: `repeated` is a virtual `Boolean` on the cont arm, which every `handleCont` answer takes, overridden in six anonymous classes. `KernelBench` in full, three legs in one session, `-f 1`, 49 rows each: the base (`dcadee780d`, the base plus the benchmark fix) against the tip, `bench/compare-base-vs-AC.md`, zero suspects; the rows outside the 5% band (`pureIterationViaArrow` -6.8%, `foreignCrossingsAnsweredInPlace` -6.2%, `continuationBodiesFuse` -5.9%, `bracketEnsuringOnly` +12.0%) are inside their combined errors and are confirmed at `-f 3` in the package's benchmark section. A first round, the base against the fix alone, agrees: `bench/compare-base-vs-A.md`, three rows outside the band, none a `handleCont` row, all noise at `-f 3` in `bench/compare-base-vs-A-f3.md`. The marker-trait alternative is not needed |
| H2 | Handler.scala:93 | `def resumed: ContHandler[I, O, E, A, A, S] = bug(...)` | claim | justified by construction, with the reach stated: only `reentering` calls `resumed` (Handler.scala:104), and only `Eval`'s cont arm calls `reentering`, under `handler.repeated`. Six sites override `repeated` at the tip, all in `ArrowEffect.scala`: the three repeated handlers (`handleContRepeated`, its recovering overload, `handleFirstRepeated`) and their three `resumed` twins, which set `repeated = true` themselves; all six override `resumed`, the twins with `this`, so a re-entry through a twin terminates on the twin without allocating. A single-shot handler reaching the default is a kernel invariant broken, which is what `bug` is for |

Not flagged and worth a line: the `outer =>` self alias in the three expansions is what lets the
twin's `run` delegate to the handler's own rather than duplicate the clause body, which is the
"overloads delegate to canonical" rule applied to an anonymous class.

Also not flagged: the `inline` on `attachReentryUnlessSettled` and its twin. The two fused walks are
themselves `inline` templates expanded at every `handleLoop` and `handleLoopState` site, and their
size is a design property; a non-inline helper on the tail would add a call inside every expansion.
`inline` keeps the expansion the branch the walks carried before. Number: the fix leg against the
tip, `KernelBench` in full, same session, `-f 1`, 49 rows, `bench/compare-A-vs-AC.md`: zero
suspects; the rows the tails sit in move `handleLoopAnswersInPlace` +0.4%,
`handleLoopFusesContinuation` +0.0%, `statefulAnswersPaySuccessor` -0.9%, and every fusion row
within 2%. The two rows outside the band, `pureIterationViaArrow` -6.3% and
`continuationBodiesFuse` -5.9%, are inside their combined errors, and are confirmed at `-f 3` in the
package's benchmark section.
