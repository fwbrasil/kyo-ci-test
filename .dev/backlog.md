# Backlog: ci-green-followup2

Worktree `.claude/worktrees/ci-green-followup2`, branch `ci-green-followup2`, base `84d9274beb` (the user's kernel branch tip).
`origin/main` merged at `3315cb9ceb` (`c970b3c612`, no conflicts, the nine overlapping files audited). **Green CI runs: 0.** Goal: three green full runs on
`oses='linux-x64 linux-arm64 windows-x64 windows-arm64'`.

Runs: one sbt at a time, JVM only until the user says otherwise, through `scratchpad/host-sbtc.sh <log> '<cmd>'` in
the background. Whole-tree `kyoJVM/test` stalls when Metals steals a fork port: run per module. Kernel sources are the
user's: they reach the user's worktree only through a live review, one Edit at a time. Commit as Flavio Brasil.
Previous version of this file, with the full history: `git show bd0f9a285a:.dev/backlog.md`.

Status: 🔄 in progress (me) · ⏳ blocked or waiting · ❓ needs the user's ruling · 👀 watch · 🟡 not started

| # | status | item | description | area | evidence so far | next step | owner |
|---|---|---|---|---|---|---|---|
| 1 | 🔄 | Verify the `handleCont` / `handleContRepeated` split downstream | Single-shot `handleCont` dumps escaping (regions close where they resume, remainder owed and settled or drained); `handleContRepeated` holds them and re-enters a fresh region per application; `Choice.run` replays through it; release-only `Scope.run` reinstated; nested-eval double release left to Bracket's cell as on the robustness branch | kernel, prelude, core | `5f56e0af2a`: kyo-kernelJVM 1819 passed, 0 failed; ChoiceTest 34/0 (1 pending: `runStream` order, main's behavior); ScopeTest 88/0 with the 3 replay leaves live; ScopeInterruptTest 15; FiberTest 122 (1 pending: the spawn-trace diagnostic, dropped); HubTest 40; PathTest 210/0 (5 cancelled, platform legs); StreamSystemExtensionsTest 17/0; all `SBT_EXIT=0`. First six-module run found two `SyncTest` clauses resuming twice through `handleCont` (refused with `Closed` at the second shot): converted to `handleContRepeated` (`f80f3c3366`), SyncTest 47/0; no other non-kernel test uses `handleCont`. Then kyo-core, kyo-prelude, kyo-system, kyo-combinators, kyo-jsonrpc JVM: 126 suites, 3763 passed, 0 failed. After the merge of main: `checkClassNames JVM` 83 projects, no duplicates; kyo-test-api/runner/prop/snapshot 168/84/142/73 passed, 0 failed SqlClientInterruptTest against the Postgres container 9/0 after the merge (the advisory-lock leaf live) Whole-tree `kyoJVM/test` (`host-tree1.log`, 47 min, 1822 suites, `SBT_EXIT=1`): kernel, core (but CacheTest), net, http, sql-postgres, jsonrpc, prelude, system green; 18 red classes worked issue by issue, in this order: (1) CacheTest, done `eee755c87e` (marker off, 123/0 three times; CI's linux-x64 JS job at `6d44f30d` fails on the same marker, expected on every kyo-core job of that run); (2) DoctestCheckTest, done `6487da53cb` (passed alone and in the module; the count in the shared tmp dir was racy against three sibling suites; the leaf now checks the dirs that appeared are gone); (3) LeakCheckTest, in kyo-test at the user's direction: the Busy report's worker dump was one sample and came back empty while the verdict named the fiber's frame (`detect` now re-samples within the probe's budget), the idle probe read `Scheduler.loadAvg`, which sums only the regulator's current window and reads 0 for a pegged worker past it (`Scheduler.busyWorkers()` added, the probe reads it), and the leaf waits on its own fiber's frame instead of any worker inside 2 s; done `64fc8c8b67` (scheduler 215, leaf 18 alone, runner 84 under concurrent core and prelude, 120 suites `SBT_EXIT=0`); (4) kyo-pod leaks: `ContainerItTest` alone 444/0 on the restarted podman, so the leaks were the 2 s real-clock window on the daemon's asynchronous removal under load; the check now waits through `assertEventually` (`28f485d771`), 444/0 again on it (no run on main, by the user's ruling: build caches); `LocalInfileIntegrationTest` 10/0 and `Sha256PasswordIntegrationTest` 3/0 on the healthy podman, done; (5) browser and ui: all five green alone (Html 11/0, UIMountTest 5/0, TodoScenarioItTest 12/0, BrowserCoreTest 103/0, BrowserVerifyReadTest 20/0); the tree-run failures were `BrowserConnectionLostException`, a 1 m leaf timeout and `BrowserAssertionTimedOutException`, Chrome starved by the whole-tree fork storm, not test logic; the one assertion failure (Html's first `topology` read as the page lands) gets a barrier on the pushed row before the read (running, `host-ui2.log`); (5) browser and ui; (6) CI reds; kyo-ai ignored (keys). The original findings: `CacheTest` "the computing caller's interrupt fails the waiters and the next caller recomputes" (main's #1981 `pendingUntilFixed` leaf now passes on this branch); `DoctestCheckTest` "Driver lifecycle" (a `doctest-out` dir still there after `Scope.run`, counted in the shared tmp dir); `LeakCheckTest` "kyo Trace frame for a busy IOTask fiber" (passed alone earlier); `ContainerItTest` 123 failed and `LocalInfileIntegrationTest` 3 failed with `ContainerBackendUnavailableException` and 0 ms "no assertion" leaves: podman's gvproxy had died under the load (socket 59929 refused), VM killed and machine restarted, `podman ps` answers; `HtmlRendererReactiveRangesTest` 1 failed under the browser load; kyo-ai Codex/Gemini leaves (keys) | after the tree run: each failed suite alone, `CacheTest` in rounds, pod and mysql against the restarted podman; fix what fails alone | me |
| 2 | ❓ | Live review of the kernel diff | `Handler.scala` (`ContHandler.repeated`, `reentered`, `reentering`), `Eval.scala` (cont arm, newest-first drain), `ArrowEffect.scala` (`handleContRepeated`, scaladocs), `ContextEffect.scala` (release doc), `BracketTest`, `ContextEffectTest` | kernel | diff of `5f56e0af2a` against `84d9274beb` for those files; kernel suite green | prepare the review walked in edit order once row 1 is green, then the user opens it | user |
| 3 | ❓ | Kernel tests that apply a `handleCont` continuation more than once without a guarded region | `ArrowEffectTest`, `EvalTest`, `IsolateTest`, `EffectTraceTest`, `ArrowEffectMaskTest`, `EvalCaptureTowerTest`, `ContextEffectTest`, `SyncTest` still resume twice through `handleCont`; they pass because nothing in them refuses a second entry, but the scaladoc says a second application calls for `handleContRepeated` | kernel tests | kernel suite green as is | ruling: convert them to `handleContRepeated`, or leave them as pins that a bare double resumption still works | user |
| 4 | 🔄 | Full CI run on all four OS poles, all four targets | Pushed `b34f254640..6d44f30d6f` (115 commits, no PR, no attribution text) at the user's direction; the rung 2 custom run (35733851213) was cancelled at the user's direction in favour of a full run | CI | run 35733969856: https://github.com/fwbrasil/kyo-ci-test/actions/runs/35733969856, in progress at `6d44f30d`. Red so far: linux-x64 JS and Wasm, linux-arm64 JVM, JS and Wasm, every one on `kyo.CacheTest` alone (the marker leaf, fixed in `eee755c87e` after the dispatch); the linux-arm64 JVM job ran the whole tree (177 module results) with that single red | read it with `scripts/ci-logs.sh -R fwbrasil/kyo-ci-test run 35733969856 --failures`; every red goes back to rung 1; green counts toward row 7 only if the user says so (the ladder's first full run is the uncounted smoke) | me |
| 5 | ⏳ | Rung 1, JS / Native / Wasm | Rung 1 is done on the JVM per module (every kyo-test module green, ScalaTest modules `failed 0`, SQL group 1246 passed against real Postgres/MySQL/Dolt, browser+ui 3843 passed); the other platforms have not run since the Channel/Hub/Scope/kernel changes, by the user's instruction (JVM only) | all modules | kyo-core JS 1757 passed on `2f49e3ad98`, before the kernel split | the user's go, then full JS of the tree, Native and Wasm for touched modules (kernel, prelude, core, system, net, http, jsonrpc, browser, aeron, sql) | user, then me |
| 6 | ⏳ | Rung 3: Windows x64 and arm64 (JVM, JS); Native and Wasm on both Linux poles | Windows has no local target and is unverified until CI | CI | none | after row 4 is green: `-f mode=full -f targets='JVM JS' -f oses='windows-x64 windows-arm64'` and `-f targets='Native Wasm' -f oses='linux-x64 linux-arm64'` | me |
| 7 | ⏳ | Rung 4: full CI, one uncounted smoke then three counted green runs | `-f mode=full -f oses='linux-x64 linux-arm64 windows-x64 windows-arm64'`; the dispatch default omits windows-arm64. A red at any rung sends the item back to rung 1 and resets the count | CI | green count 0 | after row 6 | me |
| 8 | 👀 | kyo-http file-descriptor leak at end of run, Linux container | `LeakCheck$Detected` once at `79e8094db0` (a NIO client connection with a read armed); 14 clean runs of 14 at later HEADs; the cause was never found, so it stopped reproducing rather than being fixed | kyo-http, kyo-net | 1 red in 1 run at `79e8094db0`; 14 of 14 clean after | read every container and CI kyo-http run for it; if it reappears, rerun with `KYO_TEST_LEAK_DEBUG=1` to attribute the descriptor to a leaf, then reproduce and fix | me |
| 9 | 🟡 | Final report | `.dev/overnight-report.md` brought current after rows 1 and 4 to 7: what was found, fixed, proven, and the CI runs | docs | report current through `b99764e14a` | after the CI rungs | me |

## Standing rulings (not work; do not reopen)

- D2 `closeAwaitEmpty` forfeits a handed-back value: kept pending by the user, 3 `pendingUntilFixed` leaves in `ChannelTest` (main's behavior).
- No backpressure on abnormal scope exit ("WE'LL SHIP WITHOUT BACKPRESSURE ON FAILURE"): the pending leaves in `ScopeTest` and `StreamCoreExtensionsTest` stay as they are.
- No guarantee for interrupts during an acquire, only at its end: the two abandoned-join leaves (AsyncTest, ScopeInterruptTest) and the EvalTest stop-before-capture leaf were removed.
- `Choice.runStream` orders nested choice points breadth-first where `run` is depth-first: main's behavior, same code there; removed from the backlog by the user. The pending leaf in `ChoiceTest` stays.
- `Fiber.Unsafe.init` failure trace without the spawning chain's frames: diagnostic only, dropped from the backlog by the user. The pending leaf in `FiberTest` stays.
- D4 `Counter.get` resets on read: dropped from the backlog. D9 `NioIoDriverTest` jitter loops: left as is. Swallowed `IOException`s in kyo-net: not to be fixed.
- Do not change kyo-test. No hot loops, sleeps or spawn hooks in tests. Never touch a PR.
