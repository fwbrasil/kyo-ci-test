# Backlog: ci-green-followup2

Worktree `.claude/worktrees/ci-green-followup2`, branch `ci-green-followup2`, base `84d9274beb` (the user's kernel branch tip).
HEAD `bd0f9a285a`, 104 commits unpushed since `b34f254640`. **Green CI runs: 0.** Goal: three green full runs on
`oses='linux-x64 linux-arm64 windows-x64 windows-arm64'`.

Runs: one sbt at a time, JVM only until the user says otherwise, through `scratchpad/host-sbtc.sh <log> '<cmd>'` in
the background. Whole-tree `kyoJVM/test` stalls when Metals steals a fork port: run per module. Kernel sources are the
user's: they reach the user's worktree only through a live review, one Edit at a time. Commit as Flavio Brasil.
Previous version of this file, with the full history: `git show bd0f9a285a:.dev/backlog.md`.

Status: 🔄 in progress (me) · ⏳ blocked or waiting · ❓ needs the user's ruling · 👀 watch · 🟡 not started

| # | status | item | description | area | evidence so far | next step | owner |
|---|---|---|---|---|---|---|---|
| 1 | 🔄 | Verify the `handleCont` / `handleContRepeated` split downstream | Single-shot `handleCont` dumps escaping (regions close where they resume, remainder owed and settled or drained); `handleContRepeated` holds them and re-enters a fresh region per application; `Choice.run` replays through it; release-only `Scope.run` reinstated; nested-eval double release left to Bracket's cell as on the robustness branch | kernel, prelude, core | `5f56e0af2a`: kyo-kernelJVM 1819 passed, 0 failed; ChoiceTest 34/0 (1 pending: row 11); ScopeTest 88/0 with the 3 replay leaves live; ScopeInterruptTest 15; FiberTest 122 (1 pending: row 12); HubTest 40; PathTest 210/0 (5 cancelled, platform legs); StreamSystemExtensionsTest 17/0; all `SBT_EXIT=0`. First six-module run found two `SyncTest` clauses resuming twice through `handleCont` (refused with `Closed` at the second shot): converted to `handleContRepeated` (`f80f3c3366`), SyncTest 47/0; no other non-kernel test uses `handleCont` | kyo-core, kyo-prelude, kyo-system, kyo-combinators, kyo-jsonrpc, kyo-test JVM suites (running, `host-modules2.log`); then the tree per module on the JVM | me |
| 2 | ❓ | Live review of the kernel diff | `Handler.scala` (`ContHandler.repeated`, `reentered`, `reentering`), `Eval.scala` (cont arm, newest-first drain), `ArrowEffect.scala` (`handleContRepeated`, scaladocs), `ContextEffect.scala` (release doc), `BracketTest`, `ContextEffectTest` | kernel | diff of `5f56e0af2a` against `84d9274beb` for those files; kernel suite green | prepare the review walked in edit order once row 1 is green, then the user opens it | user |
| 3 | ❓ | Kernel tests that apply a `handleCont` continuation more than once without a guarded region | `ArrowEffectTest`, `EvalTest`, `IsolateTest`, `EffectTraceTest`, `ArrowEffectMaskTest`, `EvalCaptureTowerTest`, `ContextEffectTest`, `SyncTest` still resume twice through `handleCont`; they pass because nothing in them refuses a second entry, but the scaladoc says a second application calls for `handleContRepeated` | kernel tests | kernel suite green as is | ruling: convert them to `handleContRepeated`, or leave them as pins that a bare double resumption still works | user |
| 4 | ⏳ | Push `ci-green-followup2` to `fwbrasil/kyo-ci-test` | Every CI rung needs the branch on the fork; the harness refused `git push fork ci-green-followup2` and the retry was rejected | git | 104 commits unpushed; `git log --format=%B` of them carries no attribution text | the user pushes, or allows the push | user |
| 5 | ⏳ | Rung 1, JS / Native / Wasm | Rung 1 is done on the JVM per module (every kyo-test module green, ScalaTest modules `failed 0`, SQL group 1246 passed against real Postgres/MySQL/Dolt, browser+ui 3843 passed); the other platforms have not run since the Channel/Hub/Scope/kernel changes, by the user's instruction (JVM only) | all modules | kyo-core JS 1757 passed on `2f49e3ad98`, before the kernel split | the user's go, then full JS of the tree, Native and Wasm for touched modules (kernel, prelude, core, system, net, http, jsonrpc, browser, aeron, sql) | user, then me |
| 6 | ⏳ | Rung 2: one `mode=custom` CI dispatch with every proven suite | `gh workflow run ci-dispatch.yml --repo fwbrasil/kyo-ci-test --ref ci-green-followup2 -f mode=custom ...` | CI | none | after row 4 | me |
| 7 | ⏳ | Rung 3: Windows x64 and arm64 (JVM, JS); Native and Wasm on both Linux poles | Windows has no local target and is unverified until CI | CI | none | after row 6 | me |
| 8 | ⏳ | Rung 4: full CI, one uncounted smoke then three counted green runs | `-f mode=full -f oses='linux-x64 linux-arm64 windows-x64 windows-arm64'`; the dispatch default omits windows-arm64. A red at any rung sends the item back to rung 1 and resets the count | CI | green count 0 | after row 7 | me |
| 9 | 👀 | kyo-http file-descriptor leak at end of run, Linux container | `LeakCheck$Detected` once at `79e8094db0` (a NIO client connection with a read armed); 14 clean runs of 14 at later HEADs; the cause was never found, so it stopped reproducing rather than being fixed | kyo-http, kyo-net | 1 red in 1 run at `79e8094db0`; 14 of 14 clean after | read every container and CI kyo-http run for it; if it reappears, rerun with `KYO_TEST_LEAK_DEBUG=1` to attribute the descriptor to a leaf, then reproduce and fix | me |
| 10 | ⏳ | SQL advisory-lock `pendingUntilFixed` leaf (`SqlClientInterruptTest`) | The cause is fixed by the user's PR #1982 on main (`lockedOn` registers the release before the acquire); nothing edited here for it. When that change reaches this branch the marker must come off, or the leaf reports "now passes" | kyo-sql | leaf pending on JVM and JS against a real Postgres container | after #1982 merges and is brought in: remove the marker, run the suite in the container | user, then me |
| 11 | ❓ | `Choice.runStream` orders nested choice points breadth-first where `run` is depth-first | `runStream` emits what is settled before expanding what is pending; same code on `origin/main`; the pending leaf came with the robustness port (`26988dad11`) | prelude | ChoiceTest 1 pending | ruling: change `runStream` to the order `run` collects, or drop the leaf | user |
| 12 | ❓ | `Fiber.Unsafe.init` failure trace does not carry the spawning chain's frames | Diagnostic only; pending leaf in `FiberTest` | core, kernel EffectTrace | FiberTest 1 pending | ruling: carry the frames through `EffectTrace` at the spawn, or drop the leaf | user |
| 13 | 🟡 | Final report | `.dev/overnight-report.md` brought current after rows 1, 4 to 8: what was found, fixed, proven, and the CI runs | docs | report current through `b99764e14a` | after the CI rungs | me |

## Standing rulings (not work; do not reopen)

- D2 `closeAwaitEmpty` forfeits a handed-back value: kept pending by the user, 3 `pendingUntilFixed` leaves in `ChannelTest` (main's behavior).
- No backpressure on abnormal scope exit ("WE'LL SHIP WITHOUT BACKPRESSURE ON FAILURE"): the pending leaves in `ScopeTest` and `StreamCoreExtensionsTest` stay as they are.
- No guarantee for interrupts during an acquire, only at its end: the two abandoned-join leaves (AsyncTest, ScopeInterruptTest) and the EvalTest stop-before-capture leaf were removed.
- D4 `Counter.get` resets on read: dropped from the backlog. D9 `NioIoDriverTest` jitter loops: left as is. Swallowed `IOException`s in kyo-net: not to be fixed.
- Do not change kyo-test. No hot loops, sleeps or spawn hooks in tests. Never touch a PR.
